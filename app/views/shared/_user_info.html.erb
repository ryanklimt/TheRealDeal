<%= link_to gravatar_for(current_user), current_user %>
<h1>
  <%= current_user.username.humanize %>
</h1>
<span>
  <%= link_to "view my profile", current_user.username %>
</span>
<span>
  <%= pluralize(current_user.wallposts.count, "post") %>
</span>

