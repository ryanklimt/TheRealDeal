<%= form_for(current_user.subscriptions.find_by(subforum_id: @subforum.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unsubscribe", class: "btn btn-large btn-primary" %>
<% end %>